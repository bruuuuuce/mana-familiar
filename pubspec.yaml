name: mana_familiar
description: Desktop companion for exploring Mana learning journeys and evidence graphs.
publish_to: none
version: 0.2.1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  re_editor: ^0.10.0
  re_highlight: ^0.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/branding/mana-familiar-logo.png
    - assets/branding/mana-familiar-welcome.png
